<Window x:Class="EmoTracker.Extensions.BontaMultiworld.MultiWorldLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmoTracker.Extensions.BontaMultiworld"
        mc:Ignorable="d"
        Title="MultiWorld (Bonta) Log" Height="450" Width="800" MinWidth="350">
    <Grid>
        <Grid Background="#FF212121">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <ScrollViewer Name="LogScrollView" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <ItemsControl Name="LogHost" FontFamily="Consolas" FontSize="12" Foreground="LightGray" ItemsSource="{Binding MessageLog}" Margin="0,0,5,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" TextTrimming="None" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <Button Grid.Row="1" Content="Clear Message Log" Command="{Binding ClearMessageLogCmd}" />
        </Grid>
    </Grid>
</Window>
