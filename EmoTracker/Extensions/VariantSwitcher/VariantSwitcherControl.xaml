<UserControl x:Class="EmoTracker.Extensions.VariantSwitcher.VariantSwitcherControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:app="clr-namespace:EmoTracker"
             xmlns:data="clr-namespace:EmoTracker.Data;assembly=EmoTracker.Data"
             xmlns:local="clr-namespace:EmoTracker.Extensions.NDI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid ToolTip="Right click to switch variants for the current pack" Background="Transparent">
        <TextBlock FontFamily="{StaticResource FontAwesome5Free}" Text="&#xf07c;" Foreground="WhiteSmoke" VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ActiveGamePackage.AvailableVariants.Count, Source={x:Static data:Tracker.Instance}}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <Grid.ContextMenu>
            <ContextMenu ItemsSource="{Binding ActiveGamePackage.AvailableVariants, Source={x:Static data:Tracker.Instance}}">
                <ContextMenu.ItemContainerStyle>
                    <Style TargetType="MenuItem">
                        <Setter Property="Command" Value="{Binding ActivatePackCommand, Source={x:Static app:ApplicationModel.Instance}}" />
                        <Setter Property="CommandParameter" Value="{Binding }" />
                    </Style>
                </ContextMenu.ItemContainerStyle>
                <ContextMenu.ItemTemplate>
                    <DataTemplate>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" SharedSizeGroup="name"/>
                                <ColumnDefinition MinWidth="20" />
                                <ColumnDefinition Width="auto" SharedSizeGroup="author"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding DisplayName}" VerticalAlignment="Center"/>
                            <Border Grid.Column="3" CornerRadius="5" BorderThickness="1.5" BorderBrush="{Binding (TextBlock.Foreground), RelativeSource={RelativeSource Self}}" Margin="10,5,0,5">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="Active" FontSize="10" FontWeight="Bold" Margin="2"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ContextMenu.ItemTemplate>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</UserControl>
