<UserControl x:Class="EmoTracker.UI.ItemGridControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmoTracker.UI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="{Binding LegacyMargin}" RenderOptions.BitmapScalingMode="NearestNeighbor">
        <Grid.CacheMode>
            <BitmapCache RenderAtScale="1.0" SnapsToDevicePixels="True"/>
        </Grid.CacheMode>
        <Grid.Resources>
            <ItemsPanelTemplate x:Key="HorizontalItemsPanel">
                <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </Grid.Resources>
        <ItemsControl ItemsSource="{Binding Path=Rows}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid MinHeight="10">
                        <ItemsControl ItemsSource="{Binding}" HorizontalAlignment="Center" ItemsPanel="{StaticResource HorizontalItemsPanel}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <local:TrackableItemControl DataContext="{Binding}"
                                                                Margin="{Binding DataContext.ItemMargin, RelativeSource={RelativeSource AncestorType=local:ItemGridControl, Mode=FindAncestor}}"
                                                                IconWidth="{Binding DataContext.ItemWidth, RelativeSource={RelativeSource AncestorType=local:ItemGridControl, Mode=FindAncestor}}"
                                                                IconHeight="{Binding DataContext.ItemHeight, RelativeSource={RelativeSource AncestorType=local:ItemGridControl, Mode=FindAncestor}}"
                                                                BadgeFontSize="{Binding DataContext.BadgeFontSize, RelativeSource={RelativeSource AncestorType=local:ItemGridControl, Mode=FindAncestor}}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>            
    </Grid>
</UserControl>
